val it = () : unit
signature SIG = sig end
functor F(<param>: sig end) : sig end
functor G(<param>: sig end) :
         sig
           structure A : <sig>
           structure CMSA : <sig>
         end
structure B :
  sig
    structure A : <sig>
    structure CMSA : <sig>
  end

